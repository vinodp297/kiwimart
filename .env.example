# .env.local  —  KiwiMart Sprint 3 Environment Variables
# Copy this file to .env.local and fill in all values before running.
# NEVER commit .env.local to git (it's in .gitignore by default in Next.js).
#
# See: https://nextjs.org/docs/app/building-your-application/configuring/environment-variables

# ─────────────────────────────────────────────────────────────────────────────
# App
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production: NEXT_PUBLIC_APP_URL=https://buyzi.co.nz

# ─────────────────────────────────────────────────────────────────────────────
# App identity — brand name and legal entity shown in emails and UI
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME=Buyzi
COMPANY_LEGAL_NAME=Buyzi Limited
COMPANY_ADDRESS=Auckland, New Zealand

# ─────────────────────────────────────────────────────────────────────────────
# Contact — support email shown in error messages and emails (NEXT_PUBLIC so
# it can be rendered in client components without an extra API call)
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPPORT_EMAIL=support@buyzi.co.nz

# ─────────────────────────────────────────────────────────────────────────────
# Buyer protection — the maximum amount covered by buyer protection, shown
# as a trust signal across the UI and in emails
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_BUYER_PROTECTION_AMOUNT=3000
NEXT_PUBLIC_BUYER_PROTECTION_DISPLAY=$3,000

# ─────────────────────────────────────────────────────────────────────────────
# Order rules — time limits and file caps that drive business logic.
# Must be kept in sync with PlatformConfig table when that is built.
# ─────────────────────────────────────────────────────────────────────────────
# Minutes after order placement in which a buyer can cancel for free (no seller approval)
FREE_CANCEL_WINDOW_MINUTES=60
# Hours after creation that an offer expires
OFFER_EXPIRY_HOURS=72
# Hours after offer acceptance for buyer to complete purchase
OFFER_PURCHASE_WINDOW_HOURS=24
# Maximum number of evidence photos a party can upload per dispute
DISPUTE_EVIDENCE_MAX_FILES=4

# ─────────────────────────────────────────────────────────────────────────────
# Processing times — shown in transactional emails to set expectations.
# Update these when your Stripe settlement terms change.
# ─────────────────────────────────────────────────────────────────────────────
REFUND_PROCESSING_DAYS_MIN=5
REFUND_PROCESSING_DAYS_MAX=10
PAYOUT_PROCESSING_DAYS_MIN=2
PAYOUT_PROCESSING_DAYS_MAX=3
# Days after return approval that buyer must ship item back
RETURN_SHIPPING_WINDOW_DAYS=7

# ─────────────────────────────────────────────────────────────────────────────
# Policy paths — URL paths for policy pages linked in emails
# ─────────────────────────────────────────────────────────────────────────────
LISTING_POLICY_PATH=/policies/listing-guidelines

# ─────────────────────────────────────────────────────────────────────────────
# Database — Neon PostgreSQL
# Get from: https://console.neon.tech → your project → Connection Details
#
# DATABASE_URL         → Prisma Accelerate pooled URL (for app queries)
# DATABASE_DIRECT_URL  → Direct Neon URL (for migrations only)
# ─────────────────────────────────────────────────────────────────────────────
DATABASE_URL="prisma://accelerate.prisma-data.net/?api_key=REPLACE_WITH_PRISMA_ACCELERATE_KEY"
DATABASE_DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"

# Without Prisma Accelerate (local dev only — use direct URL for both):
# DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"
# DATABASE_DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"

# ─────────────────────────────────────────────────────────────────────────────
# Auth.js v5
# Generate NEXTAUTH_SECRET: openssl rand -base64 32
# ─────────────────────────────────────────────────────────────────────────────
NEXTAUTH_SECRET="REPLACE_WITH_RANDOM_32_BYTE_BASE64_STRING"
NEXTAUTH_URL="http://localhost:3000"
# Production: NEXTAUTH_URL="https://kiwimart.co.nz"

# Google OAuth (optional — for Google sign-in)
# Create at: https://console.cloud.google.com → Credentials → OAuth 2.0 Client ID
# Authorised redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID="REPLACE_WITH_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="REPLACE_WITH_GOOGLE_CLIENT_SECRET"

# ─────────────────────────────────────────────────────────────────────────────
# Cloudflare Turnstile (bot protection on login/register forms)
# Get from: https://dash.cloudflare.com → Turnstile → Add Site
# Use site key "1x00000000000000000000AA" for testing (always passes)
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_TURNSTILE_SITE_KEY="1x00000000000000000000AA"
CLOUDFLARE_TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"

# ─────────────────────────────────────────────────────────────────────────────
# Stripe
# Get from: https://dashboard.stripe.com → Developers → API Keys
# Use test keys (sk_test_...) for development
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_REPLACE"
STRIPE_SECRET_KEY="sk_test_REPLACE"
# Webhook secret from: stripe listen --forward-to localhost:3000/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET="whsec_REPLACE"

# ─────────────────────────────────────────────────────────────────────────────
# Resend (transactional email)
# Email provider: Resend (https://resend.com)
# Used for: transactional emails, email digest notifications
# Get from: https://resend.com/api-keys
# ─────────────────────────────────────────────────────────────────────────────
RESEND_API_KEY=your_resend_api_key_here
EMAIL_FROM="KiwiMart <noreply@kiwimart.co.nz>"

# ─────────────────────────────────────────────────────────────────────────────
# Cloudflare R2 (image storage)
# Get from: https://dash.cloudflare.com → R2 → Manage R2 API Tokens
# ─────────────────────────────────────────────────────────────────────────────
CLOUDFLARE_ACCOUNT_ID="REPLACE_WITH_ACCOUNT_ID"
R2_BUCKET_NAME="kiwimart-listings"
R2_ACCESS_KEY_ID="REPLACE_WITH_R2_ACCESS_KEY"
R2_SECRET_ACCESS_KEY="REPLACE_WITH_R2_SECRET_KEY"
# Public R2 domain (set up custom domain in R2 dashboard)
NEXT_PUBLIC_R2_PUBLIC_URL="https://r2.kiwimart.co.nz"

# ─────────────────────────────────────────────────────────────────────────────
# Upstash Redis (rate limiting)
# Get from: https://console.upstash.com → Create Database → REST API
# ─────────────────────────────────────────────────────────────────────────────
UPSTASH_REDIS_REST_URL="https://REPLACE.upstash.io"
UPSTASH_REDIS_REST_TOKEN="REPLACE_WITH_UPSTASH_TOKEN"

# ─────────────────────────────────────────────────────────────────────────────
# Field-level encryption (phone numbers at rest)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ─────────────────────────────────────────────────────────────────────────────
ENCRYPTION_KEY="REPLACE_WITH_64_CHAR_HEX_STRING"

# ─────────────────────────────────────────────────────────────────────────────
# Twilio (SMS — phone verification and pickup OTP)
# Get from: https://console.twilio.com → Account Info
# Leave all three EMPTY to use dev/log mode (no real SMS sent).
# Fill all three to enable real SMS in production.
# ─────────────────────────────────────────────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ─────────────────────────────────────────────────────────────────────────────
# Sprint 4 (Pusher, BullMQ — not needed yet)
# ─────────────────────────────────────────────────────────────────────────────
# PUSHER_APP_ID=
# PUSHER_KEY=
# PUSHER_SECRET=
# PUSHER_CLUSTER=ap4
# NEXT_PUBLIC_PUSHER_KEY=
# NEXT_PUBLIC_PUSHER_CLUSTER=ap4

